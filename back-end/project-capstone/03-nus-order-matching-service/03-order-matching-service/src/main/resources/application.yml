server:
  port: 8080

bs:
  mysql:
    driver: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/order_matching_service_db?characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true
    username: root
    password: password
    pool:
      name: order-mysql

management:
  endpoints:
    web:
      exposure:
        include: "*"

cloud:
   aws:
      region:
         static: ap-southeast-2
         auto: false
      credentials:
         access-key: <ask vincent for key>
         secret-key: <ask vincent for key>
      end-point:
         uri: https://sqs.ap-southeast-2.amazonaws.com/293642347266/order_matching_buyer_queue.fifo
